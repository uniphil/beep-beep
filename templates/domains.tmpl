{{ range . }}
    <div class="domain">
        <h3><a href="/domains/{{ .Host }}">{{ .Host }}</a></h3>
        <p>{{ .Hits }}</p>
    </div>
{{ else }}
    <p>no domains.</p>
{{ end }}
<p><a href="/new-domain">add domain</a></p>
