{{ range . }}
    <div class="domain">
        <h3><a href="/domains/{{ .Host }}">{{ .Host }}</a></h3>
        <p>{{ .VisitorsEstimate }} people viewed {{ .Visits }} pages</p>
    </div>
{{ else }}
    <p>no domains.</p>
{{ end }}
<p><a href="/new-domain">add domain</a></p>
